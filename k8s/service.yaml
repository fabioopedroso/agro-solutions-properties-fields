apiVersion: v1
kind: Service
metadata:
  name: properties-api
  namespace: agrosolutions
  labels:
    app: properties-api
    component: backend
spec:
  type: ClusterIP
  selector:
    app: properties-api
  ports:
  - port: 8080
    targetPort: 8080
    name: http
    protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: properties-api-external
  namespace: agrosolutions
  labels:
    app: properties-api
    component: backend
spec:
  type: NodePort
  selector:
    app: properties-api
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30002
    name: http
    protocol: TCP
